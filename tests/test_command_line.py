from isign_base_test import IsignBaseTest
import logging

log = logging.getLogger(__name__)


class TestCommandLine(IsignBaseTest):
    """ TODO - Testing the command line parser """
    # likely we should refactor the argument parsing out of `bin/isign` so it can be tested separately,
    pass
